# action.yml
name: 'Build in docker'
description: 'Builds the debian package for the service in a docker container'

on: [push]
jobs:
 build:
  runs-on: ubuntu-latest
  steps:
   - name: Checkout
     uses: actions/checkout@v4
   
   - name: Containerized Build
     uses: ./.github/workflows/dockerbuild

# runs:
#   using: 'docker'
#   image: 'Dockerfile'