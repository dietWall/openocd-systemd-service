[Unit]
Description=Runs openocd with arguments file placed at %i
After=network.target

[Service]
Type=simple
ExecStart=/usr/bin/openocd-starter -f %i
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target